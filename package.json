{
  "name": "codex-subagents-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal MCP server providing Claude-style sub-agents (reviewer, debugger, security) for Codex CLI.",
  "main": "dist/codex-subagents.mcp.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/codex-subagents.mcp.ts",
    "start": "node dist/codex-subagents.mcp.js",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "e2e": "tsx scripts/e2e-demo.ts"
  },
  "keywords": [
    "mcp",
    "codex",
    "subagents",
    "anthropic",
    "claude",
    "tools"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "yaml": "^2.5.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.2",
    "vitest": "^1.6.0"
  }
}
